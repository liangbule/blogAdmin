/***
 * 创建Virtual DOM
 * @param {string} type 类型
 * @param {object || null } props 属性
 * @param {createElement[]} children 属性
 * @return {object} Virtual Dom
 */
export default function createElement(type, props, ...children) {
    return (type, props, children)
}